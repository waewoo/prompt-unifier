# Pytest and Mocking Standards

- Use pytest as primary testing framework.  
- Use pytest-mock for mocking external dependencies.  
- Tests organized under `tests/`, mirrored structure.  
- Tests should be isolated, deterministic, and clear.