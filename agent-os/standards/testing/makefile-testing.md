# Makefile Testing Targets

- Support targets:  
  - `make test` runs tests.  
  - `make cov-xml` generates coverage xml for CI.  
  - `make test-html` creates html coverage report.  
- Ensure local and CI parity.